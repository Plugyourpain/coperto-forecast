# Architecture — Coperto Forecast

## 1. Goal

Прогноз числа гостей по ресторану на 7 дней вперёд.
Вход: история + (date, restaurant_id). Выход: 7-дневный прогноз + метрики качества.

## 2. Data

- Синтетический дневной ряд, 18 месяцев, 2 ресторана (генерируется скриптом).
- Схема: `date` (datetime64), `restaurant_id` (int64), `guests` (float64 из-за NaN),
  `revenue` (float64).
- Паттерны: тренд, недельная и годовая сезонность, праздничные всплески, шум.
- Три РАЗНЫХ типа пропусков (обрабатываются по-разному):
  1. **Закрытый день** — guests = 0, revenue = 0 (легитимный ноль).
  2. **Сбой выгрузки** — NaN в guests/revenue (импутация, не dropna).
  3. **Точки нет в выгрузке** — строки отсутствуют (восстанавливаем календарь).
- Аномалии: один технический выброс (POS-баг) — обрабатывается явно.
  Новогодняя ночь — НЕ выброс, а событие, остаётся.

## 3. Modules

```
src/
  data.py         — загрузка, схема, восстановление календаря,
                    обработка пропусков и аномалий
  features.py     — ЕДИНАЯ функция make_features(), вызывается и в train,
                    и в predict
  validation.py   — time_split(), MAE/MAPE, сравнение с бейзлайном
  model.py        — Pipeline, обучение, save/load, кандидаты Ridge/LightGBM

predict.py        — CLI: argparse, прогноз на 7 дней, вывод в stdout/CSV
demo.py           — пошаговая демонстрация пайплайна (для презентаций,
                    не часть продакшн-кода)

Dockerfile        — образ для CLI-инференса (данные и модель монтируются)
.dockerignore     — исключает .venv, data/, models/, notebooks/, tests/

tests/
  test_data.py        — три типа пропусков, робастность аномалий,
                        восстановление календаря
  test_features.py    — отсутствие утечек (лаг не заглядывает в будущее)
  test_validation.py  — разбиение по времени, MAE/MAPE, бейзлайн

notebooks/
  01_eda.ipynb                    — разбор данных и выводы
  02_modeling.ipynb               — сравнение моделей, разбор ошибок
  03_forecast_walkthrough.ipynb   — как устроен инференс пошагово
```

## 4. Anti-leakage rules (жёсткие)

- Все лаги и rolling — со сдвигом минимум на `horizon` (по умолчанию 7).
  Не `shift(1)`, а `shift(horizon)` — см. раздел 7.
- Split только по времени, без shuffle.
- Imputer/Scaler обучаются ТОЛЬКО на train (через Pipeline).
- Признаки на инференсе считаются той же функцией, что и на обучении.

## 5. Reproducibility

- `random_state` зафиксирован в генераторе данных и во всех моделях,
  где он есть (LightGBM). Ridge детерминирован — seed ему не нужен.
- `requirements.txt` с пиннингом версий; `requirements-optional.txt`
  для опционального LightGBM.
- Скрипт обучения: `python -m src.model` (создаёт `models/model.pkl`
  и `models/metrics.json`).
- `predict.py` загружает модель, не переобучает.
- Датасеты и модель не коммитятся (`.gitignore`) — воспроизводятся
  двумя командами.

## 6. Data preparation (src/data.py)

Три типа пропусков обрабатываются раздельно:

| Случай | Признак в данных | Решение | Флаг |
|---|---|---|---|
| Закрытый день | guests=0, revenue=0 | оставить как 0 | — |
| Сбой выгрузки | строка есть, guests=NaN | ffill по точке | `was_nan` |
| Точки нет в выгрузке | строки нет в raw | восстановить календарь + ffill | `was_missing_row` |
| Аномалия (POS-баг) | guests > 5×медиана | → NaN, затем ffill | `is_anomaly` |

Импутация — только forward-fill, без заглядывания в будущее.
Первые строки, где ffill не сработал, отбрасываются: без лагов бесполезны.

Уточнение по флагам:

- `is_anomaly` — флаг **обнаружения** (устанавливается в `flag_anomalies`).
  Замена значения на ffill выполняется отдельно в `impute()`.
- `was_nan` — исходное значение отсутствовало в raw (сбой выгрузки),
  импутировано ffill.
- `was_missing_row` — строки не было в raw (точки нет в выгрузке),
  календарь восстановлен, тоже ffill.

Известное ограничение: восстановленные строки для регулярно закрытых
дней (например, понедельники ресторана 2) не распознаются как закрытые —
ffill даёт им ненулевую посещаемость. Детекция паттерна «каждый N-й
день = 0» оставлена как улучшение.

## 7. Features (src/features.py)

Единая функция `make_features(df, target, horizon=7)` вызывается и при
обучении, и при инференсе — это архитектурное требование ТЗ.

Признаки:

- Календарь: `dow`, `month`, `is_weekend`, `is_holiday` (RU_HOLIDAYS).
- Лаги: `lag_7`, `lag_14`, `lag_21`.
- Скользящие: `roll_mean_{7,28}`, `roll_std_{7,28}` — считаются после
  `shift(horizon)`.

Принцип anti-leakage: **все** лаги и rolling сдвинуты минимум на `horizon`
дней. Горизонт прогноза = 7, поэтому минимальный лаг тоже 7. Это отход
от типового примера (там `LAGS=(1,7,14)`), но позволяет обучать один
модельный объект без рекурсии и без риска утечки. Явно зафиксировано
в README как принятое решение.

Группировка: лаги и rolling считаются внутри `restaurant_id` через
`groupby.transform`, чтобы значения не перетекали между точками.

`revenue` исключён из признаков: он известен только для прошлого и
коррелирует с таргетом (r ≈ 0.96), что даёт частичную утечку целевой
переменной.

Покрытие тестами (`tests/test_features.py`):

- лаг соответствует `shift`,
- изменение дня D не влияет на признаки < D,
- изменение дня D не влияет на признаки в (D, D+horizon),
- изменение дня D влияет на признаки в D+horizon,
- нет утечки между ресторанами,
- календарные признаки корректны.

## 8. Validation (src/validation.py)

- `time_split(df, valid_weeks=6)` — последние 6 недель в holdout,
  ничего не перемешивается. Граница строгая: train ≤ cutoff, valid > cutoff.
- Метрики: MAE (по всем точкам) и MAPE (нули исключены, число исключённых
  сообщается явно — при guests=0 метрика не определена).
- `naive_baseline_predictions` — `groupby(restaurant_id).shift(7)`.
  Это тот же день недели неделю назад, требование ТЗ.
- `compare(model_metrics, baseline_metrics)` — таблица для README
  с абсолютным и относительным приростом MAE/MAPE.
- `per_restaurant_metrics` — метрики в разрезе точек (ad-hoc анализ,
  в проде не используется).

Почему MAE и MAPE: MAE — интерпретируемая единица («ошибаемся на N гостей»),
её удобно перевести в бизнес-эффект (смены, закупки). MAPE — нормированная
ошибка, позволяет сравнивать рестораны с разным масштабом. Обе метрики
считаются на одном и том же holdout и для модели, и для бейзлайна.

## 9. Model (src/model.py)

- `build_linear_pipeline`: SimpleImputer(median) → StandardScaler →
  Ridge(α=1.0). Ridge детерминирован, random_state ему не нужен.
- `_try_build_boosting_pipeline`: LightGBM (если установлен).
  Деревья не нуждаются в scaler — только imputer.
- `train`: prepare → make_features → time_split → fit кандидатов →
  выбор лучшего по MAE. Модель и feature_cols сохраняются
  в `models/model.pkl` (joblib), метрики и конфиг — в `models/metrics.json`.
- `load`: загрузка bundle, понятная ошибка если файла нет.
- `predict_for_dates`: без рекурсии, через append пустых строк +
  make_features.

Порядок операций при обучении (важно для отсутствия утечек):

1. `prepare()` — очистка, импутация, восстановление календаря;
2. `make_features()` — лаги/rolling на ПОЛНОМ ряду;
3. `time_split()` — режем готовые фичи по времени;
4. `dropna` только в train (первые дни ряда без истории);
5. `Pipeline.fit` на train → трансформеры настраиваются только на train;
6. predict на valid, метрики, сравнение с бейзлайном.

**Бейзлайн считается на полном featurized-ряду, а затем срезается по
индексам валидации.** Это гарантирует, что `lag_7` первых дней валидации
опирается на train, и метрика не «теряет» точки. Модель и бейзлайн
сравниваются на идентичном множестве строк (84 точки).

## 10. Inference

### predict.py — продакшн CLI

```
python predict.py --date YYYY-MM-DD --restaurant N [--days 7] [--out path.csv]
```

Поведение:

1. Загружает сохранённый bundle (не переобучает).
2. Проверяет, что `--days` совпадает с горизонтом обученной модели.
3. Загружает и очищает историю через `src.data.prepare` (тот же
   пайплайн, что при обучении).
4. Проверяет, что запрошенный ресторан есть в истории.
5. Проверяет, что дата прогноза > последней даты истории.
6. Строит признаки через `make_features` (та же функция, что
   при обучении).
7. Возвращает CSV или таблицу в stdout.

Коды выхода: 0 — успех, 1 — ошибка данных/модели, 2 — ошибка аргументов.
Все ошибки — понятный текст в stderr, без трейсбеков pandas.

### demo.py — демонстрация пайплайна

```
python demo.py [--restaurant N] [--date YYYY-MM-DD] [--pause]
```

Печатает 5 шагов: загрузка истории → признаки для будущих дат →
загрузка модели → прогноз → метрики. С флагом `--pause` делает паузы
между шагами (для показа на созвоне). Не часть продакшн-пайплайна.

### Docker

```bash
docker build -t coperto-forecast .

docker run --rm \
  -v "$PWD/data:/app/data" \
  -v "$PWD/models:/app/models" \
  coperto-forecast --date 2025-10-15 --restaurant 1
```

Образ не обучает модель и не генерирует данные — только инференс.
Данные и `model.pkl` монтируются через volume. Это соответствует
практике: в образе нет ни датасета, ни обученных весов.

## 11. EDA findings → feature choices

Что показал EDA (`notebooks/01_eda.ipynb`) и как это повлияло на код:

| Наблюдение | Следствие в коде |
|---|---|
| Недельная сезонность — главный сигнал (пик Пт-Сб, ×1.4 к Пн) | признак `dow`, `is_weekend` |
| У ресторана 2 понедельник = 0 (санитарный день) | отдельный флаг не вводим; лаги 7/14/21 статистически «подсказывают» модели ноль; точный 0 линейная модель не даёт — зафиксировано в README как ограничение |
| 31.12 = ×2.3 от медианы, остальные праздники ×1.0–1.4 | `RU_HOLIDAYS` + `is_holiday` |
| POS-баг 12.11.2024 = ×13 от медианы | флаг `is_anomaly`, замена на `ffill` (не удаление) |
| Три типа пропусков (0 / NaN / нет строки) | раздельная обработка с флагами `was_nan`, `was_missing_row` |
| `revenue` коррелирует с `guests` r ≈ 0.96, но не строго | `revenue` не входит в `feature_columns` |
| Тренд положительный, годовая сезонность слабее недельной | `month` + лаги 7/14/21 достаточно; тренд улавливается через `roll_mean` |

## 12. Что показал разбор ошибок (02_modeling.ipynb)

Не влияет на архитектуру, но фиксирует известное поведение модели:

| Наблюдение | Интерпретация |
|---|---|
| Худший день — **вторник**, а не понедельник | лаги 7/14/21 хорошо справляются с закрытыми Пн; проблема в резких переходах профиля (после низкого Пн — обычный Вт), которые линейная модель сглаживает |
| Ridge не даёт ровно 0 на закрытых днях | L2-регуляризация не позволяет выразить «if Monday then 0»; решение — отдельный классификатор «открыт/закрыт» (см. README «что бы доделал») |
| Топ-10 худших прогнозов — дни с резким отклонением от среднего профиля | принципиальное свойство Ridge: он тянет прогноз к среднему; бустинг это исправил бы, но на нашем объёме данных не выигрывает |
| `is_holiday` в коэффициентах Ridge имеет малый вес (0.46) | у нас всего 5 наблюдений `is_holiday=True` в ряду; L2-регуляризация штрафует редкие признаки — при большем числе лет вес вырос бы |

## 13. Testing

31 тест, три файла:

- `tests/test_data.py` — три типа пропусков (0 / NaN / отсутствующая строка),
  робастность аномалий (POS-баг ловится, ×2.5 всплеск — нет),
  восстановление календаря, ffill без заглядывания в будущее.
- `tests/test_features.py` — ключевой тест на отсутствие утечек:
  изменение значения в день D не влияет на признаки до D+horizon;
  изоляция между ресторанами; корректность календарных признаков.
- `tests/test_validation.py` — границы `time_split` (нет пересечения,
  нет пропущенного дня), MAE/MAPE на простых примерах,
  MAPE корректно исключает нули и сообщает их количество,
  `naive_baseline_predictions` не перетекает между ресторанами.